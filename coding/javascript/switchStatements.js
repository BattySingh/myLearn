let myName = prompt("Enter your name: ");

switch(myName) {
  case "goku":
    console.log(myName + ' is so powerful');
    break;

  case "naruto":
    console.log(myName + " is a ninja");
    break;

  default:
    console.log(myName + " sounds interesting");
}
