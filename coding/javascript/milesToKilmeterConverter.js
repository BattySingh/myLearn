// Program to convert miles to kilometers

let inputMile = prompt("Enter the number of miles?", "NaN");

let resKilometer = inputMile * 1.60934;
console.log(`${ inputMile } miles is ${ resKilometer }`);

resKilometer = Number(resKilometer);
console.log(`${ inputMile } miles is ${ resKilometer }`);
