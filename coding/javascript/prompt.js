// Using prompt to get user's name, storing it inside of a variable and displaying a greeeting to the user

// prompt() is used to get the input from the user
let userName = prompt("What is your name?", "Guest");

console.log("Welcome to the site, " + userName);
